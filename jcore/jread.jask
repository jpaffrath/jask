; Provides standard functions for reading and processing input

function readNumber()
	store read() in input
	convert input to number

	return input
end

function readList(number)
	if number smallerequals 0 return NULL else endif
	if isNumber(number) equals FALSE return NULL else endif

	store list() in list

	store 0 in i
	while i smaller number
		assign listAdd(list:read()) to list
		increment i
	endrun

	return list
end